"use client"

import * as React from "react"

// Theme toggle disabled - app uses light theme only
export function ThemeToggle() {
  // Return null to hide the theme toggle completely
  return null;
}