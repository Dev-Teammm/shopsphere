/**
 * Global currency configuration for the customer app.
 * Change this to switch display currency across the entire app (e.g. RWF, USD, EUR).
 */
export const CURRENCY_CODE = "RWF" as const;
export const CURRENCY_SYMBOL = "RWF";
export const CURRENCY_LOCALE = "en-RW";
